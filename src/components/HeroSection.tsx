export default function HeroSection() {
  return (
    <header className="flex-1 flex flex-col items-center justify-center px-6 py-16 md:py-24 relative">
      {/* Placeholder — hero content built separately */}
    </header>
  );
}
